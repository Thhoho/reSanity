先使用 `skill` 工具显式加载 `resanity`，确认加载成功后再继续。你正在运行封闭语料语义回归：只允许读取当前工作区 `evidence/` 中的文件，禁止 Web、外部语料、模型记忆和文件写入；不要读取验证规则或其他工作区。宿主硬上限为 12 次总工具调用、0 次 Web 搜索、60000 非缓存 token、420 秒；尽量一次批量读取全部证据，不重试失败动作。最终只交付研究结论，不自报调用数、token、耗时或预算状态。

as-of 为 `2026-08-14`。请判断下面两个命题是否成立：

1. “澄芯科技全部官方材料都没有出现 HBM/HBM3E”；
2. “澄芯科技自身已经形成 HBM 产品、客户订单和收入”。

必须分别回答“术语是否在材料中出现”和“出现是否属于公司自身商业化证据”，给出证据标签、对象/层级、`检索结论` 与 `现实边界`。不得把披露沉默量化为经济贡献零值。
