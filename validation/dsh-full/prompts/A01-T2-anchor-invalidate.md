先使用 `skill` 工具显式加载 `resanity`。这是同一课题的第三个全新会话：只读已有 `anchors/`、`anchors/index.md`、`journal/decisions.md`，以及新到达的 `evidence/T2.md`；禁止 Web、外部语料和模型记忆。宿主硬上限为 14 次工具调用、0 Web、70000 非缓存 token、420 秒；不自报运行计量。

以 `2026-12-31` 为新 as-of 检验原锚。若新一手事件推翻了该合同形成经营暴露的条件，必须在原锚上标 `[失效]` 并追加推翻履历，不得删除、无痕改写或用新叙事把失败说成通过；同步 index 和决策日志。允许提出一个更一般的新锚，但活跃锚不超过 5。最终在聊天中说明失效事实和下一步。只允许修改锚和决策日志文件。
