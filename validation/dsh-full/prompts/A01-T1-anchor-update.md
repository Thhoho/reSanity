先使用 `skill` 工具显式加载 `resanity`。这是同一课题的一个全新会话：只读已有 `anchors/`、`anchors/index.md`、`journal/decisions.md`，以及新到达的 `evidence/T1.md`；禁止 Web、外部语料和模型记忆。宿主硬上限为 14 次工具调用、0 Web、70000 非缓存 token、420 秒；不自报运行计量。

以 `2026-09-30` 为新 as-of 检验 T0 的锚。不要另起一套重复锚：在原文件追加检验履历，更新证据等级/下一触发器和 index，并在决策日志追加一行。区分“验收延期”与“合同不存在”；保留全部历史。最终在聊天中说明哪条锚被更新、为什么、现在唯一合理动作是什么。只允许修改锚和决策日志文件。
