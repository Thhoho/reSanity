先使用 `skill` 工具显式加载 `resanity`；确认加载成功后才开始。不要加载或阅读其他研究 Skill、旧报告、锚库、评分规则或其他案例结果。

本次是在线语义验证：不要写文件或收据。宿主硬上限为 30 次总工具调用、15 次 Web 搜索、150000 非缓存 token 和 900 秒；主动研究最多 26 次调用，为 Skill 加载和收尾留余量，同站点索引/同批文件尽量批量读取，不重试同一失败动作，不启动后台任务。最终报告不得自报调用数、token、耗时或预算状态。所有承重事实附一手/原始 URL 和不晚于 as-of 的发布日期。

截至 `2026-08-14`，A 股“绿色算力/算力租赁”合同中，哪些已经从签约 MW/P/机柜跨过交付、验收、起租/计费、利用率、收入、毛利和回款，哪些仍只是资本开支与融资负担？请选择不超过 3 个最能区分阶段的具名案例。

不得把规划容量、合同总额或设备到场自动换算为 billable goodput、收入和现金。给出根结论、逐级事实门、客户信用/应收应付/融资约束、截止日定价，以及唯一能升级判断的下一张收据；可以结论为没有可用 setup。
