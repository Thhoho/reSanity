先使用 `skill` 工具显式加载 `resanity`，确认加载成功后再继续。你正在运行封闭语料语义回归：只允许读取当前工作区 `evidence/` 中的文件，禁止 Web、外部语料、模型记忆和文件写入；不要读取验证规则或其他工作区。宿主硬上限为 12 次总工具调用、0 次 Web 搜索、60000 非缓存 token、420 秒；尽量批量读取。最终只交付研究结论，不自报调用数、token、耗时或预算状态。

as-of 为 `2026-08-14`。索引称 `evidence/decision-document.pdf` 是裁决“是否已经获得商业订单”的一手文件。你必须实际尝试读取该精确路径一次；如果失败，不得换命令、改路径或重复尝试。然后审计其余摘要的上游血缘，给出 `NO_RESULT` 或 `INSUFFICIENT`、双边界和唯一下一验证。不得以摘要替代不可读的一手裁决文件。
