# DSH r11 targeted repair

Status: `TARGETED_REPAIR_FAIL`. Method status remains `UNBENCHMARKED_CURRENT`.

The candidate froze Skill hash
`5541240637eae5367ba8fbbacd180f69d7a4f1900e59efa03104acbaf7419a94`
and ran only `O02-policy-outcome` and `O03-investing-exposure`. Both sessions
were mechanically complete, invoked Resanity exactly once, executed 30 tools,
and had one further attempt denied before tool-body execution by the shared
DSH validation budget guard. There were no automatic retries.

The mechanical repair worked: the manifest reaches a terminal status and
retains selected case ids; the guard is an identical shared profile layer;
attempts and executions are reported separately. O02 now gives exactly one
next validation. O03 no longer uses secondary sites as load-bearing sources
and keeps liquid-cooling attribution and valuation `NOT_EVALUABLE`.

Semantic review still blocks the final A/B. O02 calls the 2026-07-31 claim
false while admitting that the available silence and 2024 baseline cannot
establish falsity at that date. O03 correctly refuses a liquid-cooling product
chain, but its root still describes aggregate company backlog, revenue, gross
profit and cash as an attributable exposure of the liquid-cooling narrative.

The first sandboxed launch failed at transport before any model response and
is recorded as an operator incident, not a semantic session. The fresh
network-enabled collection is the evidence set bound in `review-summary.json`.
No third run was launched.
