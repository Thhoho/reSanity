先使用 `skill` 工具显式加载 `trade-nothing`；确认加载成功后才开始。把下面任务按 `-deepthink2` 执行，研究轮次预算上限为 3。不要加载或阅读其他投资研究 Skill、旧报告、锚库、评分规则或其他案例结果。

30 分钟内一次完成。工具失败后不要自动重跑同一动作；不要向用户追问，不要生成订单、仓位、目标价或写入长期锚。最终只交付中文核验报告，正文尽量控制在 2500 中文字符以内（证据表不计），所有决定性事实附一手/原始来源 URL 与发布日期。

任务：

截至 `2026-08-14`，仅凭公开一手材料，能否证实长鑫存储已经实现 HBM 的规模量产，并获得具名头部客户的商业订单？

不要回答“技术上有可能”，只回答公开证据能支持到哪一级：样品、封装、良率、客户验证、订单、出货或收入。必须说明检查过的官方/原始信息位置、检索词和时间范围；媒体转述和多篇同源转载不能升级为独立证据。若不能证实，给出有边界的 `NO_RESULT` 或 `INSUFFICIENT`、仍缺的承重证据，以及最早可观察、成本最低的下一验证信号。
